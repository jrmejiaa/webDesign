    // There are three ways to create a variable 
    var nombre = "Jairo Mejia";
    // In JavaScript it is possible to change the type of the variable. It is very flexible to make those changes
    console.log(nombre);

    let name;
    console.log(name);

    let name = "Jairo Mejia";

    let html =  `<ul>
                    <li> ${name}
                    <li> Mi nombre es ${name}
                </ul>`